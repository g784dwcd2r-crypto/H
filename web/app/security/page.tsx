import Link from "next/link";
import styles from "@/components/Marketing.module.css";
export const metadata={title:"Security and access"};
const controls=[{title:"Your account, your sessions.",text:"Email sign-in uses a time-limited, single-use link. Google sign-in is available when configured. Signed sessions are held in HttpOnly cookies, and the account security page lets you inspect and revoke sessions."},{title:"Research with defined access.",text:"Personal projects belong to your account. Organisation projects check membership and permissions on the server. Revision checks help prevent a colleague’s changes from being silently overwritten."},{title:"Separate administration.",text:"Platform operators use a separate sign-in. Privileged changes require recent authentication and record an audit event. An ordinary account or team-owner role does not grant platform administration access."},{title:"Evidence you can inspect.",text:"Indexed documents retain version and source context. Cited research identifies exact passages for review. Generated text is still something to assess; a citation alone does not establish a claim’s correctness."}];
export default function Page(){return <div className={styles.page}><div className={styles.hero}><p className="eyebrow">Security</p><h1>Know how access works.</h1><p>Clear account boundaries, explicit project permissions and a record of privileged changes are part of the platform.</p></div>{controls.map(c=><section className={styles.section} key={c.title}><div><h2>{c.title}</h2></div><div><p>{c.text}</p></div></section>)}<section className={styles.feature}><div><h2>Discuss your requirements.</h2></div><div><p>All regions currently use Disclosure Global. Before bringing confidential material, ask us about the deployment’s hosting, providers, retention and recovery arrangements. These depend on the service configuration.</p><p className={styles.note}>This page describes implemented controls. It is not a claim of independent security certification, regional data residency or enterprise SSO.</p><Link href="/company/contact" className="btn">Contact Disclosure</Link></div></section></div>;}
